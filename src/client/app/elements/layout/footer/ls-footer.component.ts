import { BaseComponent } from 'frameworks';

@BaseComponent({
    moduleId: module.id,
    selector: 'ls-footer',
    templateUrl: 'ls-footer.html',
    styleUrls: ['footer.css']
})
export class LsFooter {}
